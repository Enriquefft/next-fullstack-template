# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.mjs" and
# the main ".env" file should be updated accordingly.

# Project info
# The project name is used to create the DB schema
NEXT_PUBLIC_PROJECT_NAME=''

# Drizzle Database URLs
# Multiple Neon branches for different environments
#
# DEVELOPMENT WORKFLOW:
#   - Use `bun run db:push` to sync schema changes directly (no migration files)
#   - Ideal for rapid prototyping and local development
#
# PRODUCTION WORKFLOW:
#   1. Make schema changes in src/db/schema/
#   2. Run `bun run db:generate` to create migration files
#   3. Review generated SQL in drizzle/ folder
#   4. Commit migration files to git
#   5. Deploy (migrations run via `bun run db:migrate` in deployment)
#
# See .claude/docs/deployment.md for platform-specific deployment guides
DATABASE_URL_DEV='postgresql://YOUR_DEV_BRANCH_URL_HERE'
DATABASE_URL_TEST='postgresql://YOUR_TEST_BRANCH_URL_HERE'
DATABASE_URL_STAGING='postgresql://YOUR_STAGING_BRANCH_URL_HERE'
DATABASE_URL_PROD='postgresql://YOUR_PROD_BRANCH_URL_HERE'

# For production deployments, set this variable on your hosting platform
# DATABASE_URL='postgresql://YOUR_PRODUCTION_URL'

# Google OAuth
GOOGLE_CLIENT_ID=''
GOOGLE_CLIENT_SECRET=''

# BetterAuth
BETTER_AUTH_SECRET='' # Random generated string, you can run bun auth:secret

# Posthog
NEXT_PUBLIC_POSTHOG_KEY=''
POSTHOG_PROJECT_ID=''

# Polar
POLAR_ACCESS_TOKEN=''
POLAR_MODE='' # 'production' or 'sandbox'

# Kapso (WhatsApp Integration)
KAPSO_API_KEY='' # Get your API key from https://app.kapso.ai

# UploadThing
UPLOADTHING_TOKEN=''

# Optional

# NEXT_PUBLIC_APP_URL='http://localhost:3000'

# BETTER_AUTH_URL=http://localhost:3000 # callback URL of your app
