# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.mjs" and
# the main ".env" file should be updated accordingly.

# Project info
# The project name is used to create the DB schema
NEXT_PUBLIC_PROJECT_NAME=''

# Drizzle Database URLs (Neon PostgreSQL)
#
# LOCAL DEVELOPMENT:
#   DATABASE_URL_DEV  - Used when running `bun dev`
#   DATABASE_URL_TEST - Used when running `bun run test:e2e`
#
# DEPLOYMENT (configured via setup-env.sh):
#   DATABASE_URL is set per-environment on Vercel (production/preview)
#
# Get a free database at https://neon.tech
DATABASE_URL_DEV='postgresql://YOUR_DEV_BRANCH_URL_HERE'
DATABASE_URL_TEST='postgresql://YOUR_TEST_BRANCH_URL_HERE'

# Google OAuth
GOOGLE_CLIENT_ID=''
GOOGLE_CLIENT_SECRET=''

# BetterAuth
BETTER_AUTH_SECRET='' # Random generated string, you can run bun auth:secret

# Posthog
NEXT_PUBLIC_POSTHOG_KEY=''
POSTHOG_PROJECT_ID=''

# Polar
POLAR_ACCESS_TOKEN=''
POLAR_MODE='' # 'production' or 'sandbox'

# UploadThing
UPLOADTHING_TOKEN=''

# Kapso WhatsApp API (optional)
# Get your API key from https://kapso.ai
KAPSO_API_KEY=''
KAPSO_PHONE_NUMBER_ID=''
META_APP_SECRET='' # For webhook signature verification

# Optional

# NEXT_PUBLIC_APP_URL='http://localhost:3000'

# BETTER_AUTH_URL=http://localhost:3000 # callback URL of your app
