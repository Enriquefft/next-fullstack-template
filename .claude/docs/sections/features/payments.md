### Payments (Polar)

**Polar** integration for payments and subscriptions.

- `src/lib/polar.ts` – Polar SDK client
- Configuration via `POLAR_ACCESS_TOKEN` and `POLAR_MODE` (sandbox/production)
- Integrated with Better Auth for user management
- Product catalog, checkout, and customer portal functionality